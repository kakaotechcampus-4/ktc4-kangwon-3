# 🤝 프로젝트 협업 & 컨벤션 가이드

모든 팀원은 아래 규칙을 준수하여 브랜치, 이슈, PR, 커밋을 관리합니다.

---

## 🏷️ 접두사 (파트 구분)
모든 이슈 및 PR 제목 앞에는 해당 파트 태그를 필수로 붙입니다.
- `[FE]` : Frontend
- `[BE]` : Backend
- `[AI]` : AI Model
- `[COMMON]` : root 폴더 작업 시

---

## 🌿 브랜치 전략
- **기본 브랜치:** `develop` (기능 개발의 기준)
- **배포 브랜치:** `main` (멘토 리뷰 및 최종 결과물)
- **브랜치 명명 규칙:**
    - `feature/{part}/{기능명}` (예: `feature/be/auth-jwt`, `feature/ai/agent`)
    - `refactor/{part}/{작업명}`

---

## 📌 이슈(Issue) 규칙
- **제목:** `[파트] 작업 내용 요약`
    - 예: `[BE] JWT 로그인 및 토큰 갱신 기능 구현`
- 내용 : 개발할 기능 또는 버그 사항을 정리합니다. 

---

## 🚀 풀 리퀘스트(PR) 규칙
- **제목:** `[파트] 태그: 작업 내용 요약`
    - 예: `[BE] feature: JWT 로그인 인증 로직 구현`
- **Base 브랜치:**
    - 기능 개발: `develop`
    - 주차별 멘토 리뷰 제출: `main` (PM 담당)
- Pull Request는 최소 기능 단위로 세분화합니다.
- Pull Request의 변경은 1,000줄을 넘기지 않도록 합니다. (테스트 코드 작성 시에는 예외) [읽어보면 좋은 자료](https://blog.banksalad.com/tech/banksalad-code-review-culture/)
- 기능 개발 후 develop을 base 브랜치로 하여 PR을 생성합니다.

---

## 💬 커밋 규칙
커밋에는 별도의 파트 태그를 붙이지 않고 표준 키워드만 사용합니다.
- Commit은 최소 변경 단위로 세분화합니다.

| 태그         | 설명                            |
|:-----------|:------------------------------|
| `feature`  | 새로운 기능 추가                     |
| `refactor` | 코드 리팩토링                       |
| `fix`      | 버그 및 오류 수정                    |
| `docs`     | 문서 수정                         |
| `style`    | 코드 포맷팅, 세미콜론 누락, 코드 변경이 없는 경우 |
| `chore`    | 빌드 업무, 패키지 매니저, 설정 파일 수정      |
| `test`     | 테스트 코드 추가 또는 수정               |
| `move`     | 파일, 폴더명을 수정하거나 옮긴 경우          |

- **예시:**
    - `feature: Refresh Token 재발급 API 추가`

